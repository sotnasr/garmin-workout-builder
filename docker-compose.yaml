version: '3.8'

services:
  garmin-workout-builder-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - GARMIN_CLIENT_ID=your_value
      - GARMIN_CLIENT_SECRET=your_value
